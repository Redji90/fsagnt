# Telegram-бот для анализа результатов фигурного катания

Telegram-бот с интеграцией LLM (OpenAI GPT) для анализа результатов соревнований по фигурному катанию из PDF файлов.

## 🏅 Возможности

- 📄 Автоматическое извлечение данных из PDF файлов с результатами
- 🤖 Анализ результатов через LLM (GPT-4/GPT-3.5)
- 📊 Структурированный анализ с выявлением топ-3 спортсменов
- 📈 Детальная статистика по категориям и дисциплинам
- 💾 Сохранение анализов для последующего использования

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key

## 🚀 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Redji90/fsagnt.git
cd fsagnt
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

4. Заполните `.env` файл вашими токенами:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4
MAX_PDF_SIZE_MB=10
```

## ▶️ Запуск

```bash
python bot.py
```

## 💬 Использование

После запуска бота в Telegram:

1. **Отправьте PDF файл** с результатами соревнований
2. Бот автоматически:
   - Извлечет текст и таблицы из PDF
   - Проанализирует результаты через LLM
   - Предоставит подробный анализ

### Доступные команды:

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/analyze` - Инструкция по анализу файлов

### Что бот анализирует:

✅ Название и дату соревнований  
✅ Категории и дисциплины (мужское/женское одиночное, парное, танцы на льду)  
✅ Топ-3 спортсменов в каждой категории  
✅ Достижения и рекорды  
✅ Ключевые моменты и интересные факты  
✅ Сравнение результатов (если доступно)  

## 📁 Структура проекта

```
.
├── bot.py              # Основной файл бота с обработчиками команд
├── config.py           # Конфигурация и настройки
├── pdf_parser.py       # Модуль для парсинга PDF файлов
├── llm_analyzer.py     # Модуль для анализа через LLM
├── requirements.txt    # Зависимости проекта
├── .env.example        # Пример конфигурации
├── .gitignore         # Игнорируемые файлы
├── uploads/           # Временные загруженные PDF файлы
├── analysis/          # Сохраненные анализы
└── README.md          # Документация
```

## 🔧 Технологии

- **python-telegram-bot** - Telegram Bot API
- **OpenAI API** - LLM для анализа
- **pdfplumber** / **PyPDF2** - Парсинг PDF файлов
- **python-dotenv** - Управление переменными окружения

## ⚙️ Настройки

В файле `.env` можно настроить:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (обязательно)
- `OPENAI_API_KEY` - API ключ OpenAI (обязательно)
- `OPENAI_MODEL` - модель для использования (по умолчанию: gpt-4)
- `MAX_PDF_SIZE_MB` - максимальный размер PDF файла в МБ (по умолчанию: 10)

## 📝 Примеры использования

1. Получите PDF файл с результатами соревнований
2. Отправьте файл боту в Telegram
3. Дождитесь обработки (10-30 секунд)
4. Получите подробный анализ результатов

## ⚠️ Ограничения

- Максимальный размер PDF файла: 10 МБ (настраивается)
- PDF должен содержать текст (не только изображения)
- Требуется стабильное интернет-соединение для работы с OpenAI API

## 🔄 Сброс настроек

Если нужно сбросить настройки бота (очистить временные файлы и анализы):

```bash
python reset_settings.py
```

Скрипт выполнит:
- ✅ Очистит директории `uploads/` и `analysis/`
- ✅ Предложит удалить файл `.env` (если нужно)
- ✅ Создаст `.env.example` (если его нет)

После сброса:
1. Создайте новый `.env` файл на основе `.env.example`
2. Заполните токены и ключи
3. Запустите бота: `python bot.py`

## 🐛 Решение проблем

**Бот не отвечает:**
- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен

**Ошибка при обработке PDF:**
- Проверьте формат файла (должен быть PDF)
- Убедитесь, что файл содержит текст (не только изображения)
- Проверьте размер файла

**Ошибка анализа:**
- Проверьте правильность OpenAI API ключа
- Убедитесь, что у вас есть доступ к выбранной модели

## 📄 Лицензия

MIT

